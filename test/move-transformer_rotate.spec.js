import MoveTransformer from "../src/move-transformer";

xdescribe('Rotate moves', () => {

  const moveTransformer = new MoveTransformer();

  // TODO manage import
  it("", () => {
    expect(moveTransformer.rotate("R", "x")).toBe("R");
  });
});
